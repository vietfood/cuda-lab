# Roofline

For each kernel, write down:

- operation count;
- bytes moved from global memory;
- arithmetic intensity;
- expected bottleneck;
- measured throughput;
- percent of peak.

If you cannot estimate the bottleneck before running the kernel, you are benchmarking without a hypothesis.

